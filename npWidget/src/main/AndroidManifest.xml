<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="npwidget.nopointer">



    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application>

        <service
            android:name="npwidget.nopointer.aider.NPNotificationService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name="npwidget.nopointer.aider.NPAccessibilityService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
        </service>
        <service
            android:name="npwidget.nopointer.ota.absimpl.htx.BluetoothLeService"
            android:enabled="true"
            android:exported="true" />
    </application>
</manifest>
