<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="demo.npwidget">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="com.vivo.permission.manage.permission.ACCESS" />

    <application
        android:name="demo.npwidget.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="demo.npwidget.MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".NpTimeCirclePickerViewActivity" />
        <activity android:name=".NpWaterViewActivity" />
        <activity android:name=".NpLineViewActivity" />
        <activity android:name=".NpLineViewActivity" />
        <activity android:name=".NpRectViewActivity" />
        <activity android:name=".NpOxWaveViewActivity" />
        <activity android:name=".NpOxWaveViewActivity" />
        <activity android:name=".NpDateTypeActivity" />

    </application>

</manifest>